<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Component List</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .list-group-item {
      padding: 15px;
      border: 1px solid #dee2e6;
      border-radius: 5px;
      margin-bottom: 10px;
      background-color: #fff;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      transition: background-color 0.3s ease;
    }

    .list-group-item:hover {
      background-color: #f8f9fa;
    }

    .component-image {
      max-width: 100px;
      max-height: 100px;
      border-radius: 5px;
      object-fit: cover;
      margin-left: 20px;
    }

    .image-container {
      max-width: 100px;
      max-height: 100px;
      display: flex;
      align-items: center;

    }

    .component-tag {
      font-size: 1.1rem;
    }

    .search-container {
      position: relative;
      /*max-width: 500px;*/
      margin: 20px auto;
      /* Center the search input */
      /*transition: all 0.3s ease;*/
      /* Smooth transition */
    }



    .search-input {
      width: 100%;
      padding: 12px 20px;
      border: 2px solid #3074d3;
      ;
      border-radius: 30px;
      background-color: #fff;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      outline: none;
      font-size: 1em;
      color: #495057;
      /* Consistent text color */
    }

    .search-input:focus {
      border: 2px solid #66a6ff;
    }
  </style>


<script src="public/script_search_by_tag.js" defer></script>

</head>

<body>
  <div class="container search-container mt-4">
    <input type="text" class="search-input" id="tagInput" placeholder="Search by tag..." value="<%= tag %>">
  </div>
  
  <div class="container mt-4" id="search-results">
    
    <ul class="list-group">
      <% if(components && components.length> 0) { %>
        <% components.forEach(component=> { %>
          <li class="list-group-item d-flex align-items-center justify-content-between" 
          data-component-id="<%= component.component.id %>"
          id="component-<%= component.component.id %>">
            <span class="component-tag">
              <%= component.component.name %>
            </span>

            <% if(component.pics && component.pics.length> 0) { %>
              <div class="image-container">
                <img src="<%= component.pics[0].pic_path %>" alt="Component Image" class="component-image" />
              </div>
              <% } %>


          </li>
          <% }) %>
            <% } else { %>
              <p>No components available.</p>
              <% } %>
    </ul>
  </div>
</body>

</html>